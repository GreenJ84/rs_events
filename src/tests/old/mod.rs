//! Legacy test suite — pre-refactor tests preserved for reference.
//! These test the old EventEmitter/EventHandler pattern and must not be executed.

mod old_event_emitter;
mod old_listener;
mod old_payloads;
